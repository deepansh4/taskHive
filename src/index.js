import React from "react";
import ReactDOM from 'react-dom';
import Home from './Home'

ReactDOM.render(<Home/>, document.getElementById("root"));

// ReactDOM.render(what to render/display, where to render/disply)
